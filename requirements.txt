# Al-Mg-Si Precipitation Simulation API
# Production dependencies for Docker deployment

# Core scientific dependencies (from kawin package)
matplotlib>=3.3
numpy>=2.0
pycalphad>=0.11
scipy
setuptools_scm[toml]>=6.0

# FastAPI and web server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0
pydantic-settings>=2.0

# Production ASGI server (optional, for gunicorn deployment)
gunicorn>=21.0
